Estimado encargado del ministerio <%= @ministry.name %>

<% if(@present_attendances.present?) %>
Esta es la lista de miembros que asistieron el <%= @attendance_date.strftime("%d/%m/%Y") %>

  Nombre 	Teléfono
  <% @present_attendances.each do |attendance| %>
  <%=attendance.member.first_name %>  <%= attendance.member.address %>    <%= attendance.member.email %>  <%= attendance.member.phone %>
  <% end %>

<% else %>
  Lamentamos informar que para esta fecha no hubo ningún miembro que asistío a <%= @ministry.name %>
<% end %>

<% if(@absent_attendances.present?) %>
Estos son los miembros que no asistieron. Por favor, comunícate con ellos!

  Nombre  Dirección     Email 	Teléfono
  <% @absent_attendances.each.each do |attendance| %>
  <%=attendance.member.first_name %>  <%= attendance.member.address %>    <%= attendance.member.email %> 	<%= attendance.member.phone %>
  <% end %>

<% else %>
  En este día hubo asistencia completa. Felicidades, buen trabajo!
<% end %>

Saluda atte. 
<%= @user.name %>
